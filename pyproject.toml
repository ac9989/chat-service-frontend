[project]
name = "chat-service-ai-frontend"
version = "0.1.0"
description = "Streamlit frontend for chat service"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "streamlit>=1.62.0",
]

[tool.uv]
package = false
